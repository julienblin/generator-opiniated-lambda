{
  "name": "<%= name %>",
  "version": "0.1.0",
  "description": "<%= description %>",
  "private": true,
  "scripts": {
    "start": "sls offline start -s",
    "start:debug": "node --inspect ./node_modules/serverless/bin/serverless offline start -s",
    "lint": "tslint --project tsconfig.json",
    "lint:fix": "tslint --project tsconfig.json --fix",
    "test": "mocha",
    "test:integration": "newman run test/integration/integration.collection.json",
    "cover": "nyc mocha",
    "deploy": "serverless deploy -s",
    "pkg": "serverless package -s"
  },
  "nyc": {
    "extension": [
      ".ts"
    ],
    "reporter": [
      "text",
      "cobertura",
      "html"
    ]
  }
}
